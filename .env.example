# Hackathon App Environment Configuration

# Backend API URL (where your Express server is hosted)
# Development: http://localhost:3000
# Production: https://api.hackathon-platform.com
API_URL=http://localhost:3000

# Remote URL for loading renderer content from CDN (optional)
# Leave empty to use local files
# Example: https://cdn.hackathon-platform.com/app/index.html
REMOTE_URL=

# Enable auto-updater (set to true in production builds)
ENABLE_AUTO_UPDATE=false

# OpenAI Configuration (for backend)
OPENAI_API_KEY=your_openai_api_key
OPENAI_BASE_URL=https://api.openai.com/v1

# GitHub Token (for auto-updater with private repos)
GH_TOKEN=your_github_token
